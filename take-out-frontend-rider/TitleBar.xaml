<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="take_out_frontend_rider.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:take_out_frontend_rider"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

     <!-- TODO: Change title bar impl to SetTitleBar() method -->
    <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Width="48" Height="48" CacheMode="BitmapCache" Click="ButtonBack_OnClick"
                Style="{StaticResource NavigationBackButtonNormalStyle}" />
        <TextBlock Grid.Column="1" CacheMode="BitmapCache" Text="clm take out rider" Style="{StaticResource CaptionTextBlockStyle}"
                   VerticalAlignment="Center" />
    </Grid>
</UserControl>